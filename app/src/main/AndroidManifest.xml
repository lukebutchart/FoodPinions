<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.stand_still.foodpinions">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/appName"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--<activity android:name=".activities.DisplayMessageActivity" />-->
        <!--<activity android:name=".xbudget.activities.CreateOutgoingActivity" />-->
        <!--<activity android:name=".xbudget.activities.ViewOutgoingsActivity" />-->
        <!--<activity android:name=".xbudget.activities.MainBudgetActivity" />-->
        <activity android:name=".activities.EditActivity" />
    </application>

</manifest>